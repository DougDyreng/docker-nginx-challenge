---
  - name: "setup"
    hosts: localhost
    connection: local 
    tasks:

    - name: Ensure nginx package is installed
      apt:
        name: nginx
        state: present
        update_cache: True
